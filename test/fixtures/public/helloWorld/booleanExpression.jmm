import ioPlus;
class HelloWorld {
    public static void main(String[] args) {
           boolean b;
           boolean a;

           a = true;
           b = false;
           result = a && b || false;
           if(result){
               ioPlus.printResult(1);
           }
           else{
               ioPlus.printResult(0);
           }
    }
}